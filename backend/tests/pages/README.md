# 真实浏览器取证用的本地页面

给 `tests/test_real_page_probe.py` 用：探测与交互的判据只在真实 DOM 上才算数，
假对象能让 `matches`、shadow 穿透、弹层是否真的打开这几件事全部「测试里通、页面上不通」。

`holdout/` 是留出集：调配方、改 skills 时**不许打开它**。配方一旦按某个页面调过，
它就只能证明「这个页面能过」，证不了「换个实现也能过」——留出集是唯一还能回答后者的东西。
