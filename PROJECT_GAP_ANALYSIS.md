# Easy RPA 功能缺口分析

本文只保留对产品功能和流程生成能力有决策价值的内容。

---

## 1. 当前稳定覆盖

| 场景 | 当前能力 |
| --- | --- |
| 基础网页操作 | 打开页面、点击、填表、等待、滚动、截图 |
| 表格采集 | 标准表格、类表格、分页表格、加载更多 |
| 列表采集 | 相似 DOM 列表、搜索结果列表、详情链接采集 |
| 表单处理 | 文本输入、原生下拉、复选框、按键、部分 JS 赋值 |
| 登录协同 | Cookie 复用、人工输入验证码/短信码、登录失败排查 |
| 数据处理 | JSON、正则、字符串、列表、数学、类型转换 |
| 文件输出 | 文件读写、Excel 读写、CSV/JSON/Excel 导出 |
| AI 辅助 | 创建流程、修节点、跑流程、读日志、检查 DOM、审计输出 |

---

## 2. 核心短板

| 短板 | 受影响场景 | 具体表现 |
| --- | --- | --- |
| 页面模式识别缺失 | 所有页面 | AI 无法区分登录页/列表页/详情页，容易堆冗余节点、选错流程模板 |
| 复杂筛选交互弱 | 后台列表、复杂筛选页 | 日期范围、多选下拉、级联选择常出现"填了但页面状态未更新" |
| 动态等待粗糙 | 后台列表、搜索结果页 | 依赖固定等待，loading 未消失或表格未稳定时即开始提取 |
| iframe / Shadow DOM 弱 | 后台系统、Web Component 页面 | frame 上下文切换缺失，内容区在 iframe 内时流程不稳定 |
| 登录态守卫缺失 | 带登录的所有流程 | 不能区分已登录/未登录/验证码拦截/密码错误，出错后无法自恢复 |
| 图形/滑块验证码弱 | 登录、高频操作页 | 缺少区域定位、缺口分析、人工标注闭环 |
| 上传下载链路不完整 | 上传下载类页面 | 缺文件选择控件、下载完成监听、下载产物自动登记 |
| 结果校验不产品化 | 所有采集流程 | 运行成功 ≠ 业务成功，字段缺失/混入 UI 文本/筛选未生效均无感知 |
| 数据整理节点不足 | 数据输出 | 缺字段映射、去重、过滤、合并、schema 校验等表格级后处理 |

---

## 3. 补齐路线

### P0 — 直接提升流程生成成功率

| 功能 | 新增节点 | 目的 |
| --- | --- | --- |
| 页面模式识别 | — | 先识别页面类型（登录/列表/详情/筛选），再选匹配的流程模板 |
| 后台列表页模板 | — | 固化「筛选 → 稳定等待 → 提取 → 分页 → 导出」标准链路 |
| 登录态守卫 | — | 区分已登录/未登录/验证码/密码错误，并触发对应恢复动作 |
| 复杂筛选节点 | `browser.dateRangeSelect`、`browser.multiSelect`、`browser.cascaderSelect` | 覆盖日期范围、多选、级联三类高频筛选控件 |
| iframe 支持 | `browser.switchFrame` | 进入/退出 frame 上下文，解决后台系统内嵌内容区问题 |
| 结果校验前端化 | — | 明确展示行数、字段缺失率、筛选是否生效 |

### P1 — 复杂页面增强

| 功能 | 新增节点 | 目的 |
| --- | --- | --- |
| 无限滚动采集 | `browser.infiniteScrollExtract` | 支持搜索结果、商品列表、资讯流等触底加载页面 |
| 详情页循环采集 | `browser.extractDetailLoop` | 从列表页逐条进入详情页并合并字段 |
| 表格稳定等待 | `browser.waitForStableTable`、`browser.waitUntilGone` | 等 loading 消失、行数连续稳定后再提取 |
| Shadow DOM 支持 | `browser.evaluate` | 通过 JS 执行覆盖 Web Component 内部内容 |
| 上传文件 | `browser.uploadFile` | 支持文件表单与批量上传 |
| 下载监听 | `browser.waitDownload` | 监听导出按钮触发的下载，获取文件路径后续处理 |

### P2 — 验证码与人工协同

| 节点 | 用途 |
| --- | --- |
| `captcha.detect` | 判断当前页面是否进入人机验证 |
| `captcha.capture` | 截取验证码区域图像 |
| `captcha.sliderAnalyze` | 分析滑块缺口位置（像素级） |
| `captcha.sliderDrag` | 生成带加速度的拖动轨迹并执行 |
| `captcha.manualInput` | 暂停流程，等待用户手动输入文字验证码 |
| `captcha.manualClick` | 暂停流程，等待用户点选图形验证 |
| `captcha.retry` | 验证失败后刷新验证码并重试 |

### P3 — 数据输出质量

| 节点 | 用途 |
| --- | --- |
| `data.table.mapFields` | 将采集字段映射为固定 schema |
| `data.table.dedupe` | 按指定字段去重 |
| `data.table.filter` | 按条件过滤结果行 |
| `data.table.merge` | 合并列表页与详情页字段 |
| `data.schema.validate` | 校验输出结构与类型，生成缺失率/异常值报告 |

---

## 4. 结论

当前基础 Web 自动化能力已够用，后续补齐重点是三个方向：

- **页面理解**：模式识别 + 后台列表模板 + iframe/Shadow DOM，让 AI 生成流程不再"蒙"。
- **复杂交互**：复杂筛选 + 无限滚动 + 上传下载 + 验证码人工协同，覆盖当前失败率最高的场景。
- **结果可信**：筛选生效校验 + 字段质量报告 + 表格清洗，让"运行成功"等于"业务成功"。
